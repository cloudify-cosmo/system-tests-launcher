machine:
  node:
    version: 0.10.40

dependencies:
  override:
    - gem update --system
    - gem install compass
    - npm install
    - npm install -g bower grunt-cli
    - bower install

test:
  override:
    - grunt
